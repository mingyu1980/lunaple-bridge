# Next Task

Untitled

## Instructions
{
  "raw": "```json\n{\n  \"task_id\": \"2025-01-28-001\",\n  \"title\": \"Birth Chart 프론트엔드 연결 — Coming Soon 잠금 해제 (en/es/pt/ja/fr)\",\n  \"priority\": \"high\",\n  \"estimated_hours\": 3.5,\n  \"instructions\": \"## 목표\\n`functions/ai/birth-chart.js` 백엔드 API를 다국어 Birth Chart 페이지(en/es/pt/ja/fr)에 연결하고, Coming Soon 잠금을 해제한다.\\n\\n---\\n\\n## 사전 확인 (작업 전 필수)\\n\\n### 1. 현재 Coming Soon 잠금 위치 파악\\n```bash\\ngrep -r 'coming-soon\\\\|comingSoon\\\\|coming_soon\\\\|locked\\\\|LOCKED' public/ --include='*.html' --include='*.js' -l\\n```\\n- Birth Chart 관련 파일 목록 확인\\n- 잠금 조건(플래그, 변수, CSS 클래스 등) 파악\\n\\n### 2. 다국어 페이지 경로 확인\\n```bash\\nfind public/ -name '*birth*' -o -name '*chart*' | grep -v 'saju\\\\|palmistry\\\\|physiognomy\\\\|lunaple'\\n```\\n\\n---\\n\\n## 작업 순서\\n\\n### Step A — Birth Chart 입력 폼 연결\\n각 언어 페이지(en/es/pt/ja/fr)의 Birth Chart 입력 폼에서 Firebase Function 호출:\\n\\n```javascript\\n// 호출 예시 (각 페이지 JS에 적용)\\nconst response = await fetch('/api/birth-chart', {\\n  method: 'POST',\\n  headers: { 'Content-Type': 'application/json' },\\n  body: JSON.stringify({\\n    name: formData.name,\\n    year: formData.year,\\n    month: formData.month,\\n    day: formData.day,\\n    hour: formData.hour,\\n    minute: formData.minute,\\n    latitude: formData.latitude,\\n    longitude: formData.longitude,\\n    timezone: formData.timezone\\n  })\\n});\\nconst data = await response.json();\\n```\\n\\n- `/api/birth-chart` 엔드포인트는 `functions/index.js`에 이미 등록되어 있음\\n- 기존 폼 구조를 **절대 재작성하지 말 것** — 연결 코드만 추가 (30% 룰)\\n\\n### Step B — 응답 데이터 렌더링\\nProkerala 응답 구조에 맞춰 결과를 화면에 표시:\\n\\n```javascript\\n// 렌더링 대상 필드 (birth-chart.js 응답 기준)\\n// - ascendant (상승궁)\\n// - sun_sign, moon_sign\\n// - planetary_positions (행성 위치 배열)\\n// - houses (하우스 배열)\\n// 응답 구조는 functions/ai/birth-chart.js 상단 주석 또는 실제 응답 확인 후 적용\\n```\\n\\n- 결과 표시 영역이 이미 HTML에 있으면 해당 요소에 데이터 주입\\n- 없으면 **최소한의 결과 컨테이너**만 추가 (기존 레이아웃 유지)\\n\\n### Step C — Coming Soon 잠금 해제\\n- 잠금 플래그/클래스를 제거하거나 `false`로 변경\\n- 5개 언어 페이지 모두 동일하게 적용\\n- **한국어(lunaple.kr) 페이지는 절대 건드리지 말 것**\\n\\n### Step D — 로딩 & 에러 처리\\n```javascript\\n// 최소 UX\\n// 1. 로딩 스피너 표시 (API 호출 중)\\n// 2. 에러 메시지 표시 (API 실패 시)\\n// 3. 결과 표시 (성공 시)\\n// 기존 스피너/에러 컴포넌트가 있으면 재사용\\n```\\n\\n---\\n\\n## 30% 룰 체크포인트\\n- 각 수정 파일의 변경량이 기존 파일 라인 수의 30%를 초과하면 **즉시 중단하고 보고**\\n- 연결 코드(fetch + render)만 추가, 기존 로직 재작성 금지\\n\\n---\\n\\n## 완료 후 Verifier 보고 형식\\n```\\n## 수정 파일\\n- public/en/birth-chart.html (또는 .js) (+N lines)\\n- public/es/birth-chart.html (또는 .js) (+N lines)\\n- public/pt/birth-chart.html (또는 .js) (+N lines)\\n- public/ja/birth-chart.html (또는 .js) (+N lines)\\n- public/fr/birth-chart.html (또는 .js) (+N lines)\\n\\n## 30% 룰: X% 이내\\n## 신규 npm 의존성: 없음\\n## Prokerala 크레딧: 누적 N / 5,000 (월)\\n## Claude API: $0.00\\n\\n## 테스트 결과\\n- [ ] en 페이지 — 폼 제출 → API 응답 → 결과 렌더링 확인\\n- [ ] es 페이지 — 동일\\n- [ ] pt 페이지 — 동일\\n- [ ] ja 페이지 — 동일\\n- [ ] fr 페이지 — 동일\\n- [ ] Coming Soon 잠금 해제 확인 (5개 언어)\\n- [ ] 에러 케이스 (잘못된 날짜 입력) 처리 확인\\n\\n## 응답 데이터 샘플\\n(실제 API 응답 JSON 일부 첨부)\\n```\",\n  \"acceptance_criteria\": [\n    \"en/es/pt/ja/fr 5개 언어 Birth Chart 페이지에서 폼 제출 시 /api/birth-chart 호출 성공\",\n    \"Prokerala 응답 데이터(행성 위치, 상승궁 등)가 화면에 렌더링됨\",\n    \"Coming Soon 잠금이 5개 언어 페이지 모두 해제됨\",\n    \"로딩 중 스피너, 실패 시 에러 메시지 표시\",\n    \"수정 파일별 변경량 30% 이내\",\n    \"신규 npm 의존성 없음\",\n    \"한국어(lunaple.kr) 관련 파일 미접촉\",\n    \"Verifier 보고에 실제 API 응답 샘플 포함\"\n  ],\n  \"cost_estimate\": {\n    \"prokerala_credits\": 10,\n    \"claude_api_usd\": 0.0,\n    \"note\": \"테스트 호출 5~10회 예상 (언어별",
  "parseError": true
}

## Acceptance Criteria
